# Frontend

智能客服用户端，使用 React、TypeScript 和 Vite。

```bash
make frontend-sync
make frontend-dev
```

默认开发地址为 `http://localhost:5173`。

前端测试和构建：

```bash
make frontend-lint
make frontend-test
make frontend-build
make frontend-check
```
